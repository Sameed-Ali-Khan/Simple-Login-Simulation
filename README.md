# Task 3 - Simple Login Simulation (Express.js)

## Description
This project simulates a simple login API using Express.js with hardcoded credentials.

### Credentials
- **username:** admin  
- **password:** 1234  

## How to Run

### If Node.js is installed
1. Install dependencies:
2. Start the server:
3. Send POST request:
`http://localhost:3000/login`

Body:
```json
{
"username": "admin",
"password": "1234"
}
If Node.js is NOT installed (Portable Node)
Download node.exe from NodeJS website.

Place node.exe inside this folder.

Run:

pgsql
Copy code
node.exe server.js